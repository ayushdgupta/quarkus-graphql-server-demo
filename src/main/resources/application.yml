greeting:
  message: "hello"
quarkus:
  hibernate-orm:
    database:
      generation: none
    log:
      sql: true
  http:
    port: 8086
  datasource:
    db-kind: mysql
    username: root
    password: ${db_pass}
    jdbc:
      url: jdbc:mysql://localhost:3306/studen_db
      max-size: 16